from setuptools import setup, find_packages

setup(
    name="proxymesh-peer-sdk",
    version="1.0.0",
    packages=find_packages(),
    python_requires=">=3.9",
    description="ProxyMesh Python Peer SDK",
)
